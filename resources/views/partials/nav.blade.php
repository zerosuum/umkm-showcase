<nav class="sticky top-0 z-10 bg-white/80 backdrop-blur border-b">
  <div class="max-w-5xl mx-auto px-4 py-3 flex items-center gap-4">
    <a href="{{ route('home') }}" class="font-semibold">SINERGI UMKM</a>
    <a href="{{ route('umkm.index') }}">UMKM</a>
    <a href="{{ route('contact') }}">Kontak</a>
    <a href="{{ route('umkm.index') }}" class="ml-auto px-3 py-1 rounded-lg bg-blue-600 text-white">Jelajah</a>
  </div>
</nav>
